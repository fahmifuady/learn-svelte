<p>header-and-cookies/setting-headers</p>

<p>Inside a load function (as well as in form actions, hooks and API routes, which we'll learn about later) you have access to a setHeaders function, which — unsurprisingly — can be used to set headers on the response.</p>
<p>Most commonly, you'd use it to customise caching behaviour with the Cache-Control response header, but for the sake of this tutorial we'll do something less advisable and more dramatic:</p>
<p>see code in header-and-cookies/setting-headers/result/+page.server.js</p>
<p>here is the <a href="/header-and-cookies/setting-headers/result">result</a> </p>

